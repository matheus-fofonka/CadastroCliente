using CadastroCliente.Classes;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Net.Http;
using System.Web;

namespace CadastroCliente.View
{
    /* public class ClassAPIusage
     {
          using (HttpClient client = new HttpClient())
             {
                 client.BaseAddress = new Uri("http://localhost:54618/api/clientes/todos");
     var resposta = await client.GetAsync("");
     string dados = await resposta.Content.ReadAsStringAsync();

     List<ClientePOCO> clientes = new JavaScriptSerializer().Deserialize<List<ClientePOCO>>(dados);
     GridView1.DataSource = clientes;
             }

     }     
     */
}